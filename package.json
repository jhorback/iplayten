{
  "name": "ipt",
  "version": "0.1.0",
  "scripts": {
      "polyserve": "polymer serve -o",
      "polytest": "polymer test",
      "polydoc": "polymer analyze > analysis.json",
      "polybuild": "polymer build",
      "stage-push": "npm run polybuild && firebase deploy -P staging",
      "stage": "npm run polytest && npm run polybuild && firebase deploy -P staging",
      "deploy": "npm run polytest && npm run polybuild && firebase deploy -P default"
  },
  "devDependencies": {}
}
